# P01 / E00.2 — completion record

Package revision: `ENGINE-2026-09-15-R1`  
Story: E00.2 Create Engine-only scope and compatibility guards  
Owner: C (Abhinav)  
Status: **Ready for review** (not integrated; no self-merge)  
Required reviewer: Sangram Gaikwad (Engineer B)  
Preceding gate: G00 PASS at reviewed SHA `76173eb0310831529e2021bd8191e502936f2e5c`  
Integration branch: `release/engine-next`  
Story branch: `engine/P01-E00.2-scope-guards`  
Base SHA: `76173eb0310831529e2021bd8191e502936f2e5c`  
Candidate SHA: **not committed in this session.** Staging is complete under `engine/` only. The local `commit-msg` hook rejected the agent `git commit` because the Cursor git wrapper appends a `Co-authored-by: Cursor` trailer. Commit from your own authenticated git session (do not use Cursor's Create PR), then replace this line with `git rev-parse HEAD`.

Group counter remains **0**. G10–G52 remain **NOT_RUN** / **NOT_YET_DUE**. This is not a release authorization.

## Dirty inventory at start (not reset)

- `M engine/docs/implementation/gates/G00.md` — G00 PASS decision text vs committed “not yet PASS”. Included in this story because P01 requires a recorded G00 PASS SHA.

## Changed files (all under `engine/`)

- `engine/.gitignore`
- `engine/pyproject.toml`
- `engine/verification/` (scope guard, quality-command runner, inventory, YAML manifest)
- `engine/tests/verification/` (scope/quality/credential fixtures and tests)
- `engine/docs/release/pr-checklist.md`
- `engine/docs/release/quality-command-manifest.md`
- `engine/docs/release/consumer-imports.md`
- `engine/docs/implementation/gates/G00.md`
- `engine/docs/implementation/gates/P01-E00.2.md` (this record)

## Digests

| Artifact | SHA-256 |
|---|---|
| `engine/verification/quality-commands.yaml` | `98737b4411203deb9540f6bab6c27c3cb7e7b44609008fe78cc792cae775624a` |

## Acceptance-to-test map

| Acceptance | Evidence |
|---|---|
| Guard rejects edits/renames/deletions outside `engine/`, including root scripts, workflows, lockfiles, export manifest | `engine/tests/verification/test_scope_guard.py` + `fixtures/scope_cases.yaml` |
| Inspect added/untracked, rename source+dest, deletions, staged, unstaged, cumulative diff | same; CLI `verification scope` inspected 25 events on this worktree, status PASS |
| Compare to pre-existing inventory without resetting unrelated work | `preexisting-working-tree-inventory.json`; `test_preexisting_inventory_does_not_reset_unrelated_root_dirty` |
| Quality-command manifest from first story | `engine/verification/quality-commands.yaml` + `engine/docs/release/quality-command-manifest.md` |
| Tests through existing engine-tests job | Job **not present** in this clone (no `.github/workflows` CI). Local equivalent `pytest tests` recorded as product BASELINE_FAIL. Root `testpaths=["tests"]` does **not** collect `engine/tests/verification`. Root CI not edited. Harness pytest is the mandatory runner evidence. |
| Baseline failures recorded separately, not relabeled pass | Product ruff/mypy/pytest `record_as_baseline: true`; `test_baseline_failure_is_not_classified_pass` |
| No production publish credentials in normal tests | `test_no_publish_credentials.py`; runner denylist |
| Checks in `engine/verification`; regression tests in `engine/tests/verification` | layout as delivered |
| Reviewer/gate evidence under `engine/docs/implementation` | this file + G00.md |
| Outside-Engine consumer imports read-only | `engine/docs/release/consumer-imports.md` |
| PR checklist | `engine/docs/release/pr-checklist.md` |

## Commands and true exit codes (2026-09-16)

Isolated interpreter: `.venv-engine-next` Python 3.14.7  
Tools: ruff 0.16.7, mypy 1.20.2, pytest 9.1.1, pytest-cov 6.3.0, pip-audit 2.10.1 (installed into the venv from the engine extra pin; not a root `pyproject.toml` change)

| Check | Command | Exit | Classification |
|---|---|---|---|
| Scope | `python -m verification scope --base release/engine-next` | 0 | PASS (25 events, all under `engine/`) |
| Harness ruff | `python -m ruff check verification tests` (cwd `engine/`) | 0 | PASS |
| Harness mypy | `python -m mypy --config-file pyproject.toml` (cwd `engine/`) | 0 | PASS |
| Harness pytest+coverage | `pytest tests --import-mode=importlib --cov=verification --cov-branch --cov-fail-under=80 -q` | 0 | PASS; 27 passed, 0 skipped; verification coverage 85.31% (floor 80, branch measurement on) |
| pip check | `python -m pip check` (repo root) | 0 | PASS |
| Product ruff | `python -m ruff check .` (repo root) | 1 | **BASELINE_FAIL** 541 errors (273 fixable); same as G00 |
| Product mypy | `python -m mypy --config-file pyproject.toml` | 1 | **BASELINE_FAIL** 133 errors in 55 files (1259 checked); same as G00 |
| Product pytest+coverage | `pytest tests --import-mode=importlib --cov=codestrata --cov-branch --cov-fail-under=80 -q` | 2 | **BASELINE_FAIL** interrupted after 32 collection errors (`validation.*`); coverage floor not evaluated |
| pip-audit | `python -m pip_audit --progress-spinner off` | 0 | PASS (“No known vulnerabilities found”). Feed was reachable this run. |
| Root engine-tests CI job | n/a | n/a | **NOT PRESENT** in clone; not claimed as lint/type/coverage |

Skipped: none of the harness tests. Docker/remote/historical suites **NOT_YET_DUE**.

Determinism repeats (3× same pinned analyzer inputs): **NOT_YET_DUE** (no canonical assessment behavior changed).

Packaging wheel/sdist: **NOT_YET_DUE** (packaging composition of `codestrata` unchanged).

## Known limitations

1. Product package still lives at repository root (`src/codestrata`, root `pyproject.toml`). `engine/pyproject.toml` is harness-only. Policy sample commands that `cd engine/` then lint `.` do not match this checkout.
2. Root pytest will not collect `engine/tests/verification` until an out-of-scope root CI/`testpaths` change.
3. Product lint/type/test baseline remains failing; must be repaired within allowed Engine scope before G10 PASS. E00.2 does not relabel those as passing.
4. Isolated candidate output/cache directory from G00 section 4 is still missing (no assessment candidate run in this story).
5. `pip-audit` is pinned in `engine/pyproject.toml` extra `quality` and was installed locally into `.venv-engine-next`; it is not a root extra.

## Defect IDs

None opened against E00.2 harness behavior. Product baseline failures remain G00-recorded, not new passing tests.

## PR text for `release/engine-next`

Title: `E00.2: Engine-only scope guard and quality-command manifest`

Body:

```text
## Summary
- Add an Engine-owned git scope guard that rejects adds/renames/deletes outside engine/, including root scripts, workflows, lockfiles, and the export manifest, while preserving a pre-existing dirty inventory.
- Add the executable quality-command manifest (product vs harness vs pip-audit) and a PR checklist. Root CI is unchanged. Historical engine-tests is pytest-only and is not treated as lint/type/coverage evidence.
- Record G00 product baseline failures separately (ruff 541, mypy 133, pytest collection 32). Harness ruff/mypy/pytest+coverage pass.

## Test plan
- [x] PYTHONPATH=engine python -m verification scope --base release/engine-next
- [ ] From engine/: ruff, mypy, pytest tests --cov=verification --cov-branch --cov-fail-under=80
- [x] Confirm no tracked files outside engine/
- [ ] Different engineer review
```
