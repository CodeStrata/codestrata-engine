# G00 — Preflight ledger

Package revision: ENGINE-2026-09-15-R1
Date recorded: 2026-09-16
Coordinator (temporary C): Abhinav
Evidence location: `engine/docs/implementation/gates/`

## Starting point

| Ref | SHA | Timestamp |
|---|---|---|
| Planning baseline (docs pin, different repo) | `3f32ce4b82a06162062babf7e8c77451f5761e6a` | n/a |
| origin/main | `ca18a660f164f8c2bd9983a81faf1f82094a1abc` | 2026-08-14T04:54:45-07:00 |
| release/engine-next (actual checkout) | `1666b472293bc3c836f6923c45e224ff1c9e2cb0` | 2026-09-16T09:58:57+05:30 |

Drift vs planning pin: YES — different repository and later history. Treat prompt file/path assumptions as unverified.
Drift vs origin/main: YES — expected; engine docs/package commits exist on `release/engine-next`.

Dirty/untracked (do not reset):
- `M engine/docs/implementation/prompts/00-START-HERE/G00-preflight-and-branch-setup.md`
- `?? engine/docs/implementation/gates/`

## Team (temporary — confirm with Vinay)

| Role | Name | Notes |
|---|---|---|
| A | Sangram | Temporary |
| B | Sathish | Temporary |
| C | Abhinav | Temporary coordinator |

Reviewer-differs-from-author: agreed for every story. The implementing engineer does not merge their own story. Example: C-owned P01 is reviewed by A or B, not by C.

## Publisher / workflow check (before push)

This clone has no `.github/workflows/` files (only issue/PR/release-note templates). Pushing `release/engine-next` or a story branch cannot fire a GitHub Actions publish/tag/mirror workflow from this repository as currently checked out. Branch-protection rules on GitHub.com were not inspected from this session.

## Environment

- Venv: `.venv-engine-next` (Windows `Scripts/`, not Unix `bin/`)
- Interpreter: `C:\Abhinav\Codestrata\codestrata-engine\.venv-engine-next\Scripts\python.exe`
- Version: `3.14.7` — project `requires-python >=3.12`; ruff/mypy configs still target 3.12. No local 3.12 runtime (`py -3.12` failed).
- Install: `python -m pip install -e ".[dev,mcp]"` from repo root
- Isolated candidate output/cache directory: **not created**. Section 4 still requires a separate output/cache path so live/stable paths are not mutated. Do this before any assessment candidate run.

## Git identity (this clone)

- `user.name`: `abhinavbussa`
- `user.email`: `abhi.bussa@gmail.com`
- Latest commit author/committer: `abhinavbussa <abhi.bussa@gmail.com>`
- `core.hooksPath`: `engine/docs/implementation/git-hooks`

Sangram and Sathish must set their own local `user.name` / `user.email` on their clones before they commit.

## Quality-command baseline

Run from **repo root** (not `engine/`) with `.venv-engine-next` active. Recorded 2026-09-16. Failures are the baseline, not a waiver.

| Command | Exit | Result |
|---|---|---|
| `python -m pip check` | 0 | No broken requirements found |
| `python -m ruff check .` | 1 | 541 errors (273 fixable with `--fix`); mainly F401 unused imports and E501 line length > 100 |
| `python -m mypy --config-file pyproject.toml` | 1 | 133 errors in 55 files (1259 source files checked). Includes Windows-only attr-defined on `resource.getrusage` / `fcntl.flock` (POSIX APIs) |
| `python -m pytest tests --import-mode=importlib --cov=codestrata --cov-branch --cov-fail-under=80 -q` | 2 | Interrupted after **32 errors during collection** (26.16s). Typical: `ModuleNotFoundError: No module named 'validation.*'` (and related collection errors under `tests/application/runtime_performance` and `tests/benchmark`). Coverage gate did not run because collection failed |

These must be repaired within allowed Engine scope before G10 can PASS. Do not treat this G00 record as quality-gate PASS.

## Group counter and later gates

Group counter: **0** (no implementation group started; P01 not started).

| Gate | Decision |
|---|---|
| G10 | NOT_RUN |
| G20 | NOT_RUN |
| G30 | NOT_RUN |
| G40 | NOT_RUN |
| G50 | NOT_RUN |
| G52 | NOT_RUN |

Stub records: `G10.md`, `G20.md`, `G30.md`, `G40.md`, `G50.md`, `G52.md` in this folder.

## Decision

**G00 not yet PASS.** Ledger items 187–190 are recorded. Remaining before claiming G00 PASS:

1. Isolated candidate output/cache directory still missing (section 4).
2. A/B/C names are temporary until Vinay confirms (Kickoff Phase 0).
3. Package-manifest digests: `PACKAGE-MANIFEST.md` says recompute SHA-256 on `release/engine-next` and record reviewer — not re-verified in this session.
4. GitHub branch-protection / publish-route (R01) not inspected beyond local absence of workflows.
5. Quality baseline is **failing**; that is allowed for G00 recording, but G10 cannot PASS on this baseline.

P01 may start only after the team accepts the remaining items above or explicitly records them as known gaps with an owner.
